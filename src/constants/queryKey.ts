import { createQueryKeys } from '@/utils';

export const USER_KEYS = createQueryKeys('user');
export const STUDENT_KEYS = createQueryKeys('student');
export const TEACHER_KEYS = createQueryKeys('teacher');
export const MATH_KEYS = createQueryKeys('math');
